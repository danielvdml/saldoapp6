<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="prepago_app.list">
      <field name="name">prepago_app list</field>
      <field name="model">prepago_app.prepago_app</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="prepago_app.action_window">
      <field name="name">prepago_app window</field>
      <field name="res_model">prepago_app.prepago_app</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="prepago_app.action_server">
      <field name="name">prepago_app server</field>
      <field name="model_id" ref="model_prepago_app_prepago_app"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="prepago_app" id="prepago_app.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="prepago_app.menu_1" parent="prepago_app.menu_root"/>
    <menuitem name="Menu 2" id="prepago_app.menu_2" parent="prepago_app.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="prepago_app.menu_1_list" parent="prepago_app.menu_1"
              action="prepago_app.action_window"/>
    <menuitem name="Server to list" id="prepago_app" parent="prepago_app.menu_2"
              action="prepago_app.action_server"/>
    -->
  </data>
</odoo>